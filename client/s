#/bin/bash
clear

if [ "$TERM" != "screen" ]; then
	tmux  \
	new-session -s indicator \
		"./indicator.py config-local.py ; sleep 9999" \; \
	split-window -p79 \
		"./indicator.py config-remote.py ; sleep 9999" \; \
	split-window -p70 \
		"bash ; tmux kill-session -t indicator"
	exit 0
fi

curl "http://localhost:9096/?token=mem&value=1"
