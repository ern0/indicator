#/bin/bash
clear

if [ "$TERM" != "screen" ]; then
	tmux new -d -s indicator "./indicator.py config.local.py ; sleep 9999"
	tmux split-window "bash ; tmux kill-session -t indicator"
	tmux a
	exit 0
fi

curl "http://localhost:9096/?token=mem&value=1"
